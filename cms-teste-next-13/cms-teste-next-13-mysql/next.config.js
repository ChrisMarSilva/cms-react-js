/** @type {import('next').NextConfig} */
const nextConfig = {
    compress: true,
    experimental: {
        // serverActions: true,
        //     nextScriptWorkers: true,
    },
}

module.exports = nextConfig
